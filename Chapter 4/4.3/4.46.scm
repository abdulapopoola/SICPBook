; It would fail because sentences have a particular structure and pattern.
; English for example is a left to right language and has to be parsed
; as such otherwise, the parsing would result in gibberish.

; Were the language to be Arabic (a RTL language), then the parser would
; have to evaluate from right to left to get everything correctly.